# 다이나믹 프로그래밍 공부를 시작했다.
# 이 문제는 피보나치 수를 재귀로 풀었을때, 다이나믹으로 풀었을때 실행 횟수를 구하는 문제다.
# 하지만 실제로 두 가지 함수의 실행 횟수를 구한 후, 채점을 올리면 재귀함수 부분에서 시간초과로 실패하게 된다.
# 때문에 재귀함수의 실행횟수와 피보나치 수의 값이 같은것을 이용해 문제를 풀어야 했다.

# 결론적으로는 피보나치 수를 다이나믹으로 구현했을때의 실행 횟수와 n번째 피보나치 수를 구하는 문제였다.
# 의사코드가 나와있는 문제였지만 다이나믹 프로그래밍을 이해하는데 많은 도움이 되었다.

n = int(input())

count2 = 0


def fibonacci2(n) :
    global count2
    f = [0 for i in range(41)]
    f[1] = 1
    f[2] = 1
    for i in range(3,n+1,1) :
        f[i] = f[i-1] + f[i-2]
        count2 += 1
    return f[n]
    



print("{} {}".format(fibonacci2(n), count2))
